<Window x:Class="ClinicApp.Views.ShellWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Medium"
             TextElement.FontSize="14"
             Background="{DynamicResource MaterialDesignLightBackground}"
             Width="1280"
             Height="720" MinWidth="1280" MinHeight="720">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="19*"/>
        </Grid.ColumnDefinitions>
        <ContentControl Grid.Column="0" VerticalAlignment="Top" HorizontalAlignment="Left" prism:RegionManager.RegionName="MenuRegion"/>
        <ContentControl Grid.Column="1" prism:RegionManager.RegionName="MainRegion"/>
        <ContentControl Grid.ColumnSpan="2" prism:RegionManager.RegionName="AuthRegion"/>
    </Grid>
</Window>
